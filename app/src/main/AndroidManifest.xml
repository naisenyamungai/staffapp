<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.staff.staffapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/staffapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".CommunicationActivity"></activity>
        <activity android:name=".FinanceActivity" />
        <activity android:name=".ManagementActivity" />
        <activity
            android:name=".MarketingActivity"
            android:label="@string/title_activity_marketing"
            android:theme="@style/NoActionBar" />
        <activity android:name=".ItilActivity" />
        <activity android:name=".AgileActivity" />
        <activity android:name=".DevActivity" />
        <activity android:name=".CyberSecActivity" />
        <activity android:name=".DataScienceActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".BusinessSchool" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>